# Phase 8 — Plan 08-02: ScrollReveal Extension + Parallax Reduced-Motion

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Étendre l'usage de `ScrollReveal` aux sections Hero et Contact (toutes les sections home animent à l'entrée viewport), et ajouter le guard `prefers-reduced-motion` sur les deux parallax existants (`HeroImageParallax` + `GlobalBackground`).

**Architecture:** `ScrollReveal` reçoit une prop optionnelle `immediate?: boolean` : quand `true`, `isVisible` démarre à `true` au mount (skip de l'IntersectionObserver). Utilisé pour Hero qui est toujours visible au premier paint — évite un flash opacity 0 sous le LCP. Contact garde le comportement par défaut (révèle au scroll). Le composant ajoute aussi les modifiers `motion-reduce:` Tailwind pour neutraliser la transition + le state caché sous `prefers-reduced-motion`. Les deux parallax existants (`HeroImageParallax` motion-based, `GlobalBackground` rAF-based) consomment `usePrefersReducedMotion()` ; si `true`, ils rendent les enfants sans transform.

**Tech Stack:** Next.js 16, React 19, TypeScript, Tailwind v4, `motion/react` (déjà installé, utilisé par `HeroImageParallax`).

**Requirements:** MOTION-03, MOTION-04
**Files modified:**

- `src/components/scroll-reveal/ScrollReveal.tsx`
- `src/app/[locale]/page.tsx`
- `src/components/hero/HeroImageParallax.tsx`
- `src/components/global-background/GlobalBackground.tsx`

---

## Task 1: Extend `ScrollReveal` with `immediate` prop

**Files:**

- Modify: `src/components/scroll-reveal/ScrollReveal.tsx`

### Step 1: Add `immediate` to the props type + use it as initial state

- [ ] Remplace tout le contenu de `src/components/scroll-reveal/ScrollReveal.tsx` par :

```tsx
"use client";

import { useEffect, useRef, useState, type ReactNode } from "react";
import { cn } from "@/lib/utils";

type ScrollRevealProps = {
  children: ReactNode;
  className?: string;
  /**
   * When true, render in the "visible" state immediately at mount,
   * skipping the IntersectionObserver. Use for elements that are guaranteed
   * to be in the viewport at first paint (e.g. Hero) to avoid a flash and
   * preserve LCP.
   */
  immediate?: boolean;
};

export function ScrollReveal({
  children,
  className,
  immediate = false,
}: ScrollRevealProps): React.JSX.Element {
  const ref = useRef<HTMLDivElement>(null);
  const [isVisible, setIsVisible] = useState(immediate);

  useEffect(() => {
    if (immediate) return;
    const el = ref.current;
    if (!el) return;
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry?.isIntersecting) setIsVisible(true);
      },
      { rootMargin: "0px 0px -40px 0px", threshold: 0.1 },
    );
    observer.observe(el);
    return () => observer.disconnect();
  }, [immediate]);

  return (
    <div
      ref={ref}
      className={cn(
        "scroll-reveal transition-[opacity,transform] duration-700 ease-out motion-reduce:transition-none",
        isVisible
          ? "scroll-reveal-visible"
          : "scroll-reveal-hidden will-change-[opacity,transform] motion-reduce:opacity-100 motion-reduce:translate-y-0 motion-reduce:scale-100",
        className,
      )}
    >
      {children}
    </div>
  );
}
```

Changes vs original :

- Added `immediate?: boolean` to props (default `false`).
- `isVisible` initial state = `immediate` (so when `immediate=true`, the component renders visible at first paint).
- The IntersectionObserver effect early-returns when `immediate` is true (no observer attached, no work).
- Added `motion-reduce:transition-none` (kill the 700ms transition under `prefers-reduced-motion`) and `motion-reduce:opacity-100 motion-reduce:translate-y-0 motion-reduce:scale-100` on the hidden state (force the content visible even before the observer fires) — covers MOTION-03's reduced-motion clause.

### Step 2: Run lint + fmt

- [ ] Run: `npm run lint && npm run fmt:check`

Expected: 0 errors, 0 warnings.

### Step 3: Commit

- [ ] `````````````````````bash
                                                                              git add src/components/scroll-reveal/ScrollReveal.tsx
                                                                              git commit -m "feat(scroll-reveal): add 'immediate' prop for above-the-fold sections"
                                                                              ```
                                                                          ````
                                                                      `````
                                                                  ``````
                                                              ```````
                                                          ````````
                                                      `````````
                                                  ``````````
                                              ```````````
                                          ````````````
                                      `````````````
                                  ``````````````
                              ```````````````
                          ````````````````
                      `````````````````
                  ``````````````````
              ```````````````````
          ````````````````````
      `````````````````````

````

---

## Task 2: Wrap Hero and Contact with `ScrollReveal` on home

**Files:**
- Modify: `src/app/[locale]/page.tsx`

### Step 1: Wrap `HeroSection` with `immediate`

- [ ] Localise le JSX de la home (`src/app/[locale]/page.tsx` lignes 35-57). Remplace le bloc `<HeroSection ... />` (lignes 39-39) et le bloc `<ContactSection ... />` (lignes 52-56) par les wrappers.

Le résultat final attendu du `<div className="relative z-10 ...">` doit être :

```tsx
<div className="relative z-10 mx-auto grid w-full max-w-5xl gap-8 px-6 pt-28 pb-16 md:pt-32 md:pb-24 min-w-0 box-border">
  <HomeNav />
  <div id="content-start" tabIndex={-1} className="sr-only" aria-hidden />

  <ScrollReveal immediate>
    <HeroSection heroStack={heroStack} primaryContactLink={primaryContactLink} />
  </ScrollReveal>

  <ScrollReveal>
    <JourneySection />
  </ScrollReveal>
  <ScrollReveal>
    <StackSection />
  </ScrollReveal>

  <ScrollReveal>
    <PassionsSection />
  </ScrollReveal>

  <ScrollReveal>
    <ContactSection
      primaryContactLink={primaryContactLink}
      socialLinks={socialLinks}
      contactEmail={siteIdentity.contactEmail}
    />
  </ScrollReveal>
</div>
````

Justifications :

- `<ScrollReveal immediate>` autour de Hero : Hero est above-the-fold, son `isVisible` doit être `true` dès le mount pour ne pas dégrader le LCP avec un fade-in. Sous reduced-motion, le résultat est identique : visible immédiatement, pas de transition.
- `<ScrollReveal>` (sans `immediate`) autour de Contact : Contact est en bas de page, l'IntersectionObserver le révèle quand le user scrolle jusqu'à lui.
- Journey/Stack/Passions inchangés (déjà wrapped).

### Step 2: Run lint + fmt + build

- [ ] Run: `npm run lint && npm run fmt:check && npm run build`

Expected: tous OK.

### Step 3: Commit

- [ ] `````````````````````bash
                                                                              git add src/app/[locale]/page.tsx
                                                                              git commit -m "feat(home): wrap Hero + Contact with ScrollReveal (MOTION-03)"
                                                                              ```
                                                                          ````
                                                                      `````
                                                                  ``````
                                                              ```````
                                                          ````````
                                                      `````````
                                                  ``````````
                                              ```````````
                                          ````````````
                                      `````````````
                                  ``````````````
                              ```````````````
                          ````````````````
                      `````````````````
                  ``````````````````
              ```````````````````
          ````````````````````
      `````````````````````

````

---

## Task 3: Add `prefers-reduced-motion` guard to `HeroImageParallax`

**Files:**
- Modify: `src/components/hero/HeroImageParallax.tsx`

### Step 1: Read the existing file

- [ ] Vérifie le contenu actuel (5 lignes de logique, voir intel). Remplace l'intégralité de `src/components/hero/HeroImageParallax.tsx` par :

```tsx
"use client";

import { useScroll, useTransform, motion } from "motion/react";
import type { ReactNode } from "react";
import { usePrefersReducedMotion } from "@/hooks/usePrefersReducedMotion";

type HeroImageParallaxProps = {
  children: ReactNode;
};

/**
 * Subtle parallax on the hero image: slight upward translate as the user scrolls.
 *
 * Respects `prefers-reduced-motion: reduce` -- when reduced motion is requested,
 * the children render without the motion wrapper (no transform, no scroll listener).
 */
export function HeroImageParallax({ children }: HeroImageParallaxProps): React.JSX.Element {
  const prefersReducedMotion = usePrefersReducedMotion();
  const { scrollY } = useScroll();
  const y = useTransform(scrollY, [0, 400], [0, -48]);

  if (prefersReducedMotion) {
    return <>{children}</>;
  }

  return (
    <motion.div style={{ y }} className="will-change-transform">
      {children}
    </motion.div>
  );
}
````

Changes vs original :

- Import `usePrefersReducedMotion` from `@/hooks/usePrefersReducedMotion`.
- Hook called unconditionally (top-level), result used to choose the render branch.
- When reduced-motion is requested, render children in a fragment (no motion wrapper, no `y` transform, no `will-change`).
- `useScroll`/`useTransform` keep being called unconditionally (Rules of Hooks); they're cheap when their output isn't applied.

### Step 2: Run lint + fmt + build

- [ ] Run: `npm run lint && npm run fmt:check && npm run build`

Expected: tous OK.

### Step 3: Commit

- [ ] `````````````````````bash
                                                                              git add src/components/hero/HeroImageParallax.tsx
                                                                              git commit -m "feat(hero): respect prefers-reduced-motion in HeroImageParallax (MOTION-04)"
                                                                              ```
                                                                          ````
                                                                      `````
                                                                  ``````
                                                              ```````
                                                          ````````
                                                      `````````
                                                  ``````````
                                              ```````````
                                          ````````````
                                      `````````````
                                  ``````````````
                              ```````````````
                          ````````````````
                      `````````````````
                  ``````````````````
              ```````````````````
          ````````````````````
      `````````````````````

````

---

## Task 4: Add `prefers-reduced-motion` guard to `GlobalBackground` parallax

**Files:**
- Modify: `src/components/global-background/GlobalBackground.tsx`

### Step 1: Add the hook + early-return inside the parallax effect

- [ ] Ouvre `src/components/global-background/GlobalBackground.tsx`. Modifie deux endroits :

**1a.** Ajoute l'import en haut (juste après `import { useEffect, useRef, useState } from "react";`) :

```tsx
import { usePrefersReducedMotion } from "@/hooks/usePrefersReducedMotion";
````

**1b.** Dans le composant `GlobalBackground`, après la ligne `const [currentIndex, setCurrentIndex] = useState(0);` (ligne 23), ajoute :

```tsx
const prefersReducedMotion = usePrefersReducedMotion();
```

**1c.** Modifie le `useEffect` "Parallaxe au scroll" (lignes 34-69). Remplace la signature et le corps comme suit. Localise le bloc qui commence par `// Parallaxe au scroll.` et finit par `}, []);` (déclaration et dépendances) et remplace-le entièrement par :

```tsx
// Parallaxe au scroll. Disabled under prefers-reduced-motion: reset transform
// to identity and skip the listener entirely.
useEffect(() => {
  const el = wrapperRef.current;
  if (!el) return;

  if (prefersReducedMotion) {
    el.style.transform = "";
    return;
  }

  let maxOffsetPx = 0;
  let maxScroll = 1;
  let rafId: number | null = null;

  const updateLayout = (): void => {
    const innerHeight = window.innerHeight;
    maxScroll = Math.max(1, document.documentElement.scrollHeight - innerHeight);
    maxOffsetPx = innerHeight * MAX_OFFSET_VH;
  };

  const handleScroll = (): void => {
    if (rafId !== null) return;
    rafId = requestAnimationFrame(() => {
      rafId = null;
      const progress = Math.min(1, window.scrollY / maxScroll);
      el.style.transform = `translate3d(0, ${progress * maxOffsetPx}px, 0)`;
    });
  };

  updateLayout();
  handleScroll();

  window.addEventListener("scroll", handleScroll, { passive: true });
  const resizeObserver = new ResizeObserver(updateLayout);
  resizeObserver.observe(document.body);

  return () => {
    window.removeEventListener("scroll", handleScroll);
    resizeObserver.disconnect();
    if (rafId !== null) cancelAnimationFrame(rafId);
  };
}, [prefersReducedMotion]);
```

Changes vs original :

- Effect dependency array contient maintenant `[prefersReducedMotion]` (au lieu de `[]`) : si l'utilisateur change la préférence en live, l'effect se re-monte.
- Early-return quand `prefersReducedMotion` est `true` : on reset le transform pour effacer une éventuelle valeur résiduelle, puis on skip tout l'attachement (pas de listener, pas de ResizeObserver, 0 work au scroll).

### Step 2: Run lint + fmt + build

- [ ] Run: `npm run lint && npm run fmt:check && npm run build`

Expected: tous OK.

### Step 3: Commit

- [ ] `````````````````````bash
                                                                              git add src/components/global-background/GlobalBackground.tsx
                                                                              git commit -m "feat(global-background): respect prefers-reduced-motion in parallax (MOTION-04)"
                                                                              ```
                                                                          ````
                                                                      `````
                                                                  ``````
                                                              ```````
                                                          ````````
                                                      `````````
                                                  ``````````
                                              ```````````
                                          ````````````
                                      `````````````
                                  ``````````````
                              ```````````````
                          ````````````````
                      `````````````````
                  ``````````````````
              ```````````````````
          ````````````````````
      `````````````````````

```

---

## Task 5: Manual visual verification

**Files:** none — runtime check only.

### Step 1: Start dev server

- [ ] Run: `npm run dev`. Open: `http://localhost:3000/`.

### Step 2: Verify Hero reveal (no fade-in at first paint)

- [ ] Hard reload (Cmd+Shift+R). Expected : la section Hero est **immédiatement visible et opaque** dès le premier paint. Aucun fade-in. Comparaison : sur une section plus bas (Journey, Stack), tu vois le fade-in à l'entrée viewport.

### Step 3: Verify Contact reveal at scroll-in

- [ ] Scroll lentement vers le bas. Quand Contact entre dans le viewport (~40px avant le bas du viewport, défini par le `rootMargin` de ScrollReveal), la section fade-in + slide-up en 700ms.

### Step 4: Verify Journey/Stack/Passions still animate

- [ ] Aucune régression sur les sections déjà wrapped (Journey, Stack, Passions). Toutes animent à l'entrée viewport.

### Step 5: Verify reduced-motion path on reveals

- [ ] Active "Reduce motion" macOS (System Settings → Accessibility → Display → "Reduce motion" ON). Reload `/`.

Expected : toutes les sections (Hero, Journey, Stack, Passions, Contact) sont **visibles immédiatement** sans fade-in ni slide-up, **et** la transition CSS est désactivée (durée 0). Comportement attendu grâce aux modifiers `motion-reduce:transition-none` et `motion-reduce:opacity-100 motion-reduce:translate-y-0 motion-reduce:scale-100` ajoutés dans Task 1 Step 1. Toggle OFF, reload : reveals fonctionnent normalement (fade-in + slide-up 700ms).

### Step 6: Verify Hero parallax under reduced-motion

- [ ] Reduce motion ON. Reload `/`. Scroll : l'image hero ne bouge **pas** verticalement (pas de parallax). Scroll-back : pareil, statique.
- Reduce motion OFF. Reload : image hero glisse légèrement vers le haut (max ~48px) au scroll.

### Step 7: Verify GlobalBackground parallax under reduced-motion

- [ ] Reduce motion ON. Reload `/`. Scroll : le fond (slideshow d'images) ne bouge **pas** au scroll (les images restent fixées dans leur position de référence). Pas de translate-y. Le diaporama (transitions opacity entre images) continue de fonctionner — c'est attendu, le slideshow n'est pas une animation de motion au sens "reduced-motion" (c'est un crossfade, pas un mouvement).
- Reduce motion OFF. Reload : le fond se translate vers le bas progressivement au scroll (jusqu'à 28% de la hauteur viewport).

### Step 8: Verify live toggle of reduced-motion

- [ ] Sans reload, ouvre System Settings et toggle "Reduce motion" en live pendant que la home est ouverte. Le `usePrefersReducedMotion` écoute `matchMedia change` → les composants `HeroImageParallax` et `GlobalBackground` re-render avec la nouvelle préférence. Vérifie qu'au toggle ON, le parallax s'arrête immédiatement (transform reset à `""` pour GlobalBackground, branch fragment pour Hero).

### Step 9: If any check fails, fix and re-test before declaring plan done

Symptômes courants :

- Hero invisible / fait quand même un fade-in malgré `immediate` : vérifie que `useState(immediate)` est passé en initial value (Task 1 Step 1).
- Parallax Hero continue de bouger sous reduced-motion : vérifie que `if (prefersReducedMotion) return <>{children}</>;` est bien avant le `return <motion.div ...>` (Task 3 Step 1).
- GlobalBackground reste translaté après toggle ON live : assure-toi que la dépendance `[prefersReducedMotion]` est bien dans le `useEffect` (Task 4 Step 1c) — sinon l'effect ne se re-monte pas.

---

## Self-Review Checklist (run before declaring plan done)

- [ ] MOTION-03 : Hero, Journey, Stack, Passions, Contact reveal toutes via ScrollReveal (Task 2)
- [ ] MOTION-03 : Hero visible au premier paint sans fade-in (Task 5 Step 2) — pas de régression LCP
- [ ] MOTION-03 : reduced-motion désactive les transitions reveal (Task 5 Step 5)
- [ ] MOTION-04 : `HeroImageParallax` respecte reduced-motion (Task 5 Step 6)
- [ ] MOTION-04 : `GlobalBackground` parallax respecte reduced-motion (Task 5 Step 7)
- [ ] MOTION-04 : toggle live de reduced-motion réagit sans reload (Task 5 Step 8)
- [ ] Tous les commits passent `npm run lint && npm run fmt:check && npm run build`
- [ ] Pas de régression visuelle sur le diaporama GlobalBackground (crossfade continue de fonctionner sous reduced-motion — c'est OK, c'est un crossfade, pas un mouvement)
- [ ] Pas de régression sur les sections déjà animées (Journey, Stack, Passions reveals fonctionnent comme avant)

---

## Summary artifact (à créer après exécution)

À la fin, créer `.planning/phases/08-motion-home-polish/08-02-SUMMARY.md` documentant : commits, decisions (notamment si Step 5b a été nécessaire — guard reduced-motion sur ScrollReveal CSS), tests manuels effectués (incluant le toggle live macOS), perf observations (LCP avant/après le wrap Hero).
```
