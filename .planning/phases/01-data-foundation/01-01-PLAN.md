---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/stack-graph.ts
  - src/content/stack-graph.ts
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "TypeScript types for graph nodes and edges compile without errors"
    - "stack-graph.ts exports a complete dataset with all 31 technologies as nodes"
    - "Each node has one of 4 French experience levels (Expert, Avance, Intermediaire, Notions)"
    - "6 family colors exist with distinct oklch values"
    - "~30-40 edges represent meaningful cross-technology relations"
    - "Existing src/content/stack.ts and src/types/stack.ts are untouched"
  artifacts:
    - path: "src/types/stack-graph.ts"
      provides: "Graph type definitions (GraphNode, GraphEdge, StackGraph, ExperienceLevel, FamilyColor)"
      exports: ["ExperienceLevel", "FamilyColor", "GraphNode", "GraphEdge", "StackGraph"]
    - path: "src/content/stack-graph.ts"
      provides: "Complete graph dataset with 31 nodes, ~35 edges, 6 family colors"
      exports: ["stackGraph"]
  key_links:
    - from: "src/types/stack-graph.ts"
      to: "src/types/stack.ts"
      via: "import type { StackFamilyKey }"
      pattern: "import type.*StackFamilyKey.*from.*@/types/stack"
    - from: "src/content/stack-graph.ts"
      to: "src/types/stack-graph.ts"
      via: "import type { StackGraph }"
      pattern: "import type.*StackGraph.*from.*@/types/stack-graph"
    - from: "src/content/stack-graph.ts"
      to: "type safety"
      via: "as const satisfies StackGraph"
      pattern: "as const satisfies StackGraph"
---

<objective>
Create the TypeScript type definitions and content data for the stack graph: node types, edge types, 6 family colors in oklch, 31 technology nodes with experience levels and descriptions, and ~30-40 relation edges.

Purpose: Establish the complete data layer that Phase 2 rendering will consume. All graph data lives in typed content files following the project's existing content-as-code pattern.
Output: `src/types/stack-graph.ts` (types) and `src/content/stack-graph.ts` (data)
</objective>

<execution_context>
@/Users/florianlauer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/florianlauer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing types to reuse (DO NOT modify these files) -->

From src/types/stack.ts:

```typescript
export type StackFamilyKey = "frontend" | "mobile" | "backend" | "data" | "infra" | "integrations";

export type StackItem = {
  id: string;
  label: string;
};

export type StackGroup = {
  family: StackFamilyKey;
  title: string;
  items: StackItem[];
};
```

From src/content/stack.ts (31 technologies across 6 families):

```typescript
// frontend (7): react, nextjs, typescript, tailwindcss, vite, angular, scss
// mobile (3): capacitor, capacitor-plugin, bitrise
// backend (6): nodejs, nestjs, express, graphql, python-fastapi, kafka
// data (4): postgresql, mysql, drizzle, prisma
// infra (6): terraform, docker, github-actions, aws, datadog, nix
// integrations (5): hubspot, chargebee, mixpanel, intercom, contentful
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create graph type definitions and populate content data</name>
  <files>src/types/stack-graph.ts, src/content/stack-graph.ts</files>
  <action>
**Step 1 — Types file (`src/types/stack-graph.ts`):**

Create type definitions importing `StackFamilyKey` from `@/types/stack`:

- `ExperienceLevel` = `"Expert" | "Avancé" | "Intermédiaire" | "Notions"` (4 levels in French with accents per user decision)
- `FamilyColor` = `{ family: StackFamilyKey; label: string; color: string }` (color is oklch string)
- `GraphNode` = `{ id: string; label: string; family: StackFamilyKey; level: ExperienceLevel; description: string }`
- `GraphEdge` = `{ source: string; target: string }` (NO type/label/direction fields per user decision: simple connections only)
- `StackGraph` = `{ nodes: GraphNode[]; edges: GraphEdge[]; familyColors: FamilyColor[] }`

**Step 2 — Content file (`src/content/stack-graph.ts`):**

Import `StackGraph` from `@/types/stack-graph`. Export `stackGraph` with `as const satisfies StackGraph`.

**familyColors** (6 entries, oklch values from research, spread across hue wheel):
| Family | Label | oklch |
|--------|-------|-------|
| frontend | Frontend | oklch(0.75 0.18 85) |
| mobile | Mobile | oklch(0.70 0.19 55) |
| backend | Backend & API | oklch(0.68 0.16 230) |
| data | Data | oklch(0.72 0.14 195) |
| infra | Infra / DevOps | oklch(0.70 0.14 170) |
| integrations | Integrations SaaS | oklch(0.65 0.20 300) |

**nodes** (all 31 technologies from `src/content/stack.ts`):
Use exact same `id` and `label` values from `stack.ts`. Do NOT import from stack.ts — duplicate the values to keep graph self-contained (per research anti-pattern guidance).

For each node, add:

- `family`: matching the family from stack.ts
- `level`: one of the 4 French levels. Claude proposes initial attribution based on Florian's profile (senior fullstack, React/Next.js/TypeScript/Node.js ecosystem primary). Rough guide: Expert for daily-use core tools (React, Next.js, TypeScript, Node.js, Tailwind CSS), Avance for strong secondary tools (NestJS, Docker, PostgreSQL, GraphQL, Express, Vite), Intermediaire for tools used in projects but not primary (Angular, Prisma, Drizzle, AWS, Terraform, Python/FastAPI, Kafka, GitHub Actions), Notions for tools used occasionally or through team exposure (SCSS, Bitrise, MySQL, Datadog, Nix, and the SaaS integrations).
- `description`: French with English tech terms, mixed tone (factual + personal touch). Main tools get 1-2 sentences, secondary tools get 1 sentence. Example: "Ma bibliotheque UI principale — j'ai construit des dizaines de projets React depuis 2018."

**edges** (~30-40 entries, strong/obvious relations only, non-directed):
Group edges by logical clusters:

- Frontend core: react<->nextjs, react<->typescript, react<->tailwindcss, nextjs<->typescript, vite<->react, angular<->typescript, angular<->scss
- Backend core: nodejs<->nestjs, nodejs<->express, nestjs<->graphql, express<->graphql, nodejs<->typescript, python-fastapi<->graphql
- Data: postgresql<->prisma, postgresql<->drizzle, mysql<->prisma, nodejs<->kafka
- Mobile: capacitor<->react, capacitor<->capacitor-plugin, capacitor<->bitrise
- Infra: docker<->nestjs, docker<->aws, terraform<->aws, github-actions<->docker, github-actions<->aws, nix<->docker
- Cross-family: react<->nodejs, nextjs<->nodejs, nestjs<->postgresql, nestjs<->kafka, express<->postgresql, datadog<->nestjs, datadog<->aws
- Integrations: hubspot<->nestjs, chargebee<->nestjs, contentful<->nextjs, mixpanel<->react, intercom<->react

This gives approximately 35 edges. Adjust if needed to stay in ~30-40 range.

**Step 3 — Edge validation function:**

At the bottom of `src/content/stack-graph.ts`, add a dev-only assertion (not exported, runs at module load in dev):

```typescript
// Dev-only edge validation — catches orphan references at build time
if (process.env.NODE_ENV === "development") {
  const nodeIds = new Set(stackGraph.nodes.map((n) => n.id));
  for (const edge of stackGraph.edges) {
    if (!nodeIds.has(edge.source))
      throw new Error(`[stack-graph] Unknown edge source: ${edge.source}`);
    if (!nodeIds.has(edge.target))
      throw new Error(`[stack-graph] Unknown edge target: ${edge.target}`);
  }
}
```

IMPORTANT: Do NOT modify `src/content/stack.ts` or `src/types/stack.ts`. These files are read-only.
</action>
<verify>
<automated>cd /Users/florianlauer/Documents/perso/my-portfolio && npm run build && npm run lint</automated>
</verify>
<done>

- `src/types/stack-graph.ts` exports ExperienceLevel, FamilyColor, GraphNode, GraphEdge, StackGraph
- `src/content/stack-graph.ts` exports stackGraph with exactly 31 nodes, ~30-40 edges, 6 familyColors
- `npm run build` passes (TypeScript compilation confirms type safety)
- `npm run lint` passes
- Existing Arsenal section unaffected (stack.ts and types/stack.ts untouched)
  </done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate experience levels, descriptions, and relations</name>
  <action>
User reviews the generated graph data for accuracy. This checkpoint ensures:
- Experience levels match Florian's actual expertise
- Descriptions reflect personal experience and correct tone
- Technology relations are meaningful and complete
- Home page Arsenal section still works
  </action>
  <what-built>
Complete graph data model: 31 technology nodes with experience levels and descriptions in French, ~35 relation edges, 6 family colors in oklch. All type-safe via TypeScript `as const satisfies`.
  </what-built>
  <how-to-verify>
1. Open `src/content/stack-graph.ts` and review:
   - **Experience levels**: Are the Expert/Avance/Intermediaire/Notions assignments accurate for each technology?
   - **Descriptions**: Do they reflect your personal experience? Tone correct (factual + personal)?
   - **Edges**: Are the technology relations meaningful? Any missing obvious connections? Any connections that don't make sense?
   - **Family colors**: The oklch values will be visually tested in Phase 2, but check the family labels are correct.
2. Run `npm run dev` and verify the home page Arsenal section still works normally at http://localhost:3000 (existing stack.ts untouched).
3. Run `npm run build` to confirm everything compiles.
  </how-to-verify>
  <verify>Home page loads correctly, npm run build passes</verify>
  <done>User has approved or requested specific adjustments to levels, descriptions, or edges</done>
  <resume-signal>Type "approved" if the data looks good, or list specific changes needed (e.g., "change React level to Expert", "add edge between X and Y", "update description for Docker").</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes — TypeScript validates all types, `as const satisfies` catches structural errors
- `npm run lint` passes — no linting issues in new files
- 31 nodes present (count matches existing stack.ts technologies)
- ~30-40 edges present (strong relations only)
- 6 familyColors present (one per StackFamilyKey)
- No modifications to `src/content/stack.ts` or `src/types/stack.ts`
</verification>

<success_criteria>

- Graph types compile without errors (DATA-01)
- stack-graph.ts exports complete dataset with 31 nodes and meaningful relations (DATA-02)
- Every node has a valid French experience level (DATA-03)
- 6 families each have a distinct oklch color value (DATA-04)
- User has validated levels, descriptions, and relations
- Existing Arsenal section on home page continues to work
  </success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
