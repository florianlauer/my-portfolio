---
phase: 03-interactivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/stack-graph/StackGraph.tsx
  - src/components/stack-graph/GraphNode.tsx
  - src/components/stack-graph/GraphEdge.tsx
  - src/components/stack-graph/use-force-layout.ts
autonomous: true
requirements: [INTER-02, INTER-03]

must_haves:
  truths:
    - "Mouse wheel zooms the graph centered on cursor position (0.5x to 3x range)"
    - "Dragging the SVG background pans the graph viewport"
    - "Double-clicking the background resets zoom/pan to initial view with smooth transition"
    - "Dragging a node still moves the node (not pan)"
    - "Clicking a node highlights it and its direct neighbors at full opacity, dims all others to ~15%"
    - "Clicking the background clears focus, all nodes return to full opacity"
    - "Opacity changes animate over ~200ms"
  artifacts:
    - path: "src/components/stack-graph/StackGraph.tsx"
      provides: "Zoom behavior, background rect, inner <g> wrapper, focusedId + neighborSet state"
      contains: "bgRectRef"
    - path: "src/components/stack-graph/GraphNode.tsx"
      provides: "Click vs drag detection, onNodeClick callback, opacity prop"
      contains: "CLICK_THRESHOLD"
    - path: "src/components/stack-graph/GraphEdge.tsx"
      provides: "Opacity prop for focus dimming"
      contains: "opacity"
  key_links:
    - from: "src/components/stack-graph/StackGraph.tsx"
      to: "d3-zoom"
      via: "useEffect attaching zoom to bgRectRef"
      pattern: "select.*bgRectRef.*call.*zoom"
    - from: "src/components/stack-graph/GraphNode.tsx"
      to: "src/components/stack-graph/StackGraph.tsx"
      via: "onNodeClick callback on pointer up within threshold"
      pattern: "onNodeClick"
    - from: "src/components/stack-graph/StackGraph.tsx"
      to: "src/components/stack-graph/GraphNode.tsx"
      via: "neighborSet passed as prop drives opacity"
      pattern: "neighborSet"
---

<objective>
Add zoom/pan navigation and click-to-focus highlighting to the stack graph.

Purpose: These two features share the SVG event routing layer -- zoom restructures the SVG with a background `<rect>` and inner `<g>` wrapper, and click-to-focus needs click vs drag detection that interacts with the zoom event flow. Building them together avoids touching the same pointer event logic twice.

Output: d3-zoom installed, SVG restructured with zoom transform, node click detection, opacity-based focus highlighting on nodes and edges.
</objective>

<execution_context>
@/Users/florianlauer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/florianlauer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-interactivity/03-CONTEXT.md
@.planning/phases/03-interactivity/03-RESEARCH.md

@src/components/stack-graph/StackGraph.tsx
@src/components/stack-graph/GraphNode.tsx
@src/components/stack-graph/GraphEdge.tsx
@src/components/stack-graph/use-force-layout.ts
@src/types/stack-graph.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/stack-graph.ts:

```typescript
export type ExperienceLevel = "Expert" | "Avancé" | "Intermédiaire" | "Notions";
export type GraphNode = {
  id: string;
  label: string;
  family: StackFamilyKey;
  level: ExperienceLevel;
  description: string;
};
export type GraphEdge = { source: string; target: string };
export type StackGraph = { nodes: GraphNode[]; edges: GraphEdge[]; familyColors: FamilyColor[] };
```

From src/components/stack-graph/use-force-layout.ts:

```typescript
export type SimNode = GraphNode &
  SimulationNodeDatum & { x: number; y: number; vx: number; vy: number };
export function useForceLayout(
  nodes: GraphNode[],
  edges: GraphEdge[],
  width: number,
  height: number,
): { positions: SimNode[]; dragStart; dragMove; dragEnd };
```

From src/components/stack-graph/GraphNode.tsx:

```typescript
type GraphNodeProps = {
  node: GraphNodeType;
  x: number;
  y: number;
  color: string;
  onDragStart?: (id: string, x: number, y: number) => void;
  onDragMove?: (id: string, x: number, y: number) => void;
  onDragEnd?: (id: string) => void;
};
```

From src/components/stack-graph/GraphEdge.tsx:

```typescript
type GraphEdgeProps = { x1: number; y1: number; x2: number; y2: number };
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install d3-zoom, add zoom/pan to StackGraph SVG</name>
  <files>
    src/components/stack-graph/StackGraph.tsx
    src/components/stack-graph/use-force-layout.ts
  </files>
  <action>
1. Install d3-zoom: `npm install d3-zoom @types/d3-zoom`

2. In `StackGraph.tsx`, restructure the SVG to support zoom:
   - Add refs: `svgRef` (SVGSVGElement), `bgRectRef` (SVGRectElement), `innerGRef` (SVGGElement)
   - Replace the existing `<g>` wrappers with: a `<rect>` (first child, transparent, full width/height, `pointer-events="all"`, `cursor: grab`) then a single `<g ref={innerGRef}>` containing both edges and nodes
   - The `<rect>` is the zoom target; the `<g>` receives the zoom transform

3. Add zoom behavior via `useEffect` (empty deps — stable behavior):
   - Create `zoom<SVGRectElement, unknown>()` with `scaleExtent([0.5, 3])`
   - On `"zoom"` event: direct DOM mutation — `innerGRef.current.setAttribute("transform", event.transform.toString())`. Do NOT use useState for the transform (causes 60fps re-renders).
   - Store zoom behavior in `zoomRef` (useRef) for reset access
   - Attach to bgRectRef via `select(bgRectRef.current).call(zoomBehavior)`
   - Cleanup: remove zoom listeners on unmount

4. Add double-click reset on the background rect:
   - `onDoubleClick` handler calls `select(bgRectRef.current).transition().duration(300).call(zoomRef.current.transform, zoomIdentity)`
   - Disable d3-zoom's built-in dblclick.zoom behavior (it interferes): `select(bgRectRef.current).on("dblclick.zoom", null)` after attaching zoom

5. In `use-force-layout.ts`, export `simRef` (or a `getSimulation()` accessor) so StackGraph can access the simulation for future use. Also store a `transformRef` on StackGraph (useRef<ZoomTransform>) updated on every zoom event — Plan 02 needs this for tooltip positioning.

6. Keep the existing node drag completely unchanged — nodes call `e.stopPropagation()` in `onPointerDown` already, which prevents d3-zoom from capturing node drags. Verify this still works.

CRITICAL anti-patterns to avoid:

- Do NOT apply zoom to the SVG root element — apply to the background `<rect>` only
- Do NOT store ZoomTransform in useState — use ref + direct setAttribute
- Do NOT put zoom in the same `<g>` as content — background rect must be OUTSIDE the zoom-transformed `<g>`
  </action>
  <verify>
  <automated>cd /Users/florianlauer/Documents/perso/my-portfolio && npm run lint && npm run build</automated>
  </verify>
  <done> - d3-zoom and @types/d3-zoom in package.json - SVG has background rect + inner g with zoom transform - Mouse wheel zooms, drag on background pans, double-click resets - Node drag still works (not intercepted by zoom) - No React re-renders on zoom ticks (transform via ref + setAttribute)
  </done>
  </task>

<task type="auto">
  <name>Task 2: Click-to-focus highlighting on nodes and edges</name>
  <files>
    src/components/stack-graph/StackGraph.tsx
    src/components/stack-graph/GraphNode.tsx
    src/components/stack-graph/GraphEdge.tsx
  </files>
  <action>
1. In `GraphNode.tsx`, add click vs drag detection:
   - Add `pointerDownPos` ref tracking `{ x: number; y: number }` on pointerdown (clientX/clientY)
   - In existing `handlePointerUp`, compute euclidean distance from pointerdown position. If distance < 8px (CLICK_THRESHOLD constant), call `onNodeClick?.(node.id)` — this is a click, not a drag.
   - Add `onNodeClick?: (id: string) => void` to GraphNodeProps
   - Add `opacity?: number` prop. Apply via `style={{ opacity: opacity ?? 1, transition: "opacity 200ms ease" }}` on the root `<g>`. Do NOT use SVG `opacity` attribute (no CSS transition support).
   - Remove the existing SVG tooltip on hover (the `{hovered && <g>...` block at lines 144-167). Plan 02 replaces it with an HTML tooltip. Keep the `hovered` state and `onPointerEnter/Leave` for now — they'll be used by the tooltip callback.
   - Actually, add an `onHoverChange?: (id: string | null) => void` callback prop. On pointerEnter call `onHoverChange?.(node.id)`, on pointerLeave call `onHoverChange?.(null)`. Remove the local `hovered` useState — hover state lifts to StackGraph for Plan 02's tooltip.

2. In `GraphEdge.tsx`, add opacity support:
   - Add `opacity?: number` prop to GraphEdgeProps
   - Apply via `style={{ opacity: opacity ?? 1, transition: "opacity 200ms ease" }}` on the `<path>` element (alongside existing stroke attributes)
   - Keep existing strokeOpacity as the base; the focus opacity multiplies visually

3. In `StackGraph.tsx`, add focus state and adjacency computation:
   - Add `const [focusedId, setFocusedId] = useState<string | null>(null)`
   - Compute `neighborSet` via `useMemo`: if focusedId is null, return null. Otherwise build a `Set<string>` containing focusedId + all nodes connected by an edge (check both source and target).
   - Helper: `getNodeOpacity(nodeId)` — if neighborSet is null return 1, if neighborSet.has(nodeId) return 1, else return 0.15
   - Helper: `getEdgeOpacity(source, target)` — if neighborSet is null return 1, if neighborSet has BOTH source and target return 1, else return 0.15
   - Pass `opacity` prop to each `<GraphNode>` and `<GraphEdge>`
   - Pass `onNodeClick={setFocusedId}` to each `<GraphNode>`
   - On background rect: add `onClick={() => setFocusedId(null)}` to clear focus when clicking empty space
   - Ensure background rect onClick does NOT conflict with double-click reset (both should work — onClick fires on single click, onDoubleClick fires on double)
     </action>
     <verify>
     <automated>cd /Users/florianlauer/Documents/perso/my-portfolio && npm run lint && npm run build</automated>
     </verify>
     <done>
   - Clicking a node sets focusedId, highlights it + neighbors at full opacity, dims rest to 15%
   - Clicking background clears focus (all nodes return to 100%)
   - Opacity transitions are smooth (~200ms)
   - Short click vs drag correctly distinguished (8px threshold)
   - Node drag still works without triggering focus
   - SVG tooltip removed from GraphNode (replaced by hover callback for Plan 02)
     </done>
     </task>

</tasks>

<verification>
1. `npm run lint && npm run build` passes with no errors
2. Visual check: mouse wheel zooms graph, drag on background pans, double-click resets view
3. Visual check: node drag still moves the node (not pan)
4. Visual check: click node -> it + neighbors stay bright, rest dims. Click background -> all bright again.
5. Visual check: opacity transitions animate smoothly (not instant)
</verification>

<success_criteria>

- d3-zoom installed, zoom range 0.5x-3x functional
- SVG restructured with background rect (zoom/pan target) and inner g (transform wrapper)
- Click-to-focus with adjacency-based opacity dimming works
- All opacity transitions are 200ms smooth
- Existing node drag preserved
- Build and lint pass cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/03-interactivity/03-01-SUMMARY.md`
</output>
